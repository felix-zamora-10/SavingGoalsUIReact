import React from "react";
import ReactDom from "react-dom";
import MainContainer from "./Components/MainContainer";

const mainContainer = document.getElementById("root");

ReactDom.render(<MainContainer />, mainContainer);
